/**
 * Simple reusable function that explicitely returns false.
 * Commonly used as `event.preventDefault` helper.
 *
 * @returns Boolean `false`.
 */
export function returnFalse(): false {
  return false;
}
